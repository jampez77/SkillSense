# Project conventions

This is a sample Flutter + Python monorepo used only as a SkillSense test fixture.

## Testing

Run `flutter test` for widget tests and `pytest` for the Python backend before opening a PR.

## Code review

All pull requests require at least one approving review before merge.
