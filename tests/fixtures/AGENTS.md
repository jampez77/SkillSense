# Agent instructions

This is a sample fixture repo used only for SkillSense tests.

## Home Assistant integration

The `custom_components/` package follows Home Assistant's integration conventions: config flows,
coordinators, and diagnostics should have test coverage under `tests/`.
